require('dotenv').config(); const express = require('express'); const swaggerUi = require('swagger-ui-express'); const YAML = require('yamljs'); const app = express(); const port = process.env.PORT || 3000; const swaggerDocument = YAML.load('./swagger.yaml'); app.use(express.json()); app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerDocument)); const buscarRoutes = require('./infrastructure/web/routes/buscar'); app.use('/api/buscar', buscarRoutes); app.get('/', (req, res) => res.send('API Buscador Ferretería en línea')); app.listen(port, () => console.log('Servidor corriendo en http://localhost:' + port));
