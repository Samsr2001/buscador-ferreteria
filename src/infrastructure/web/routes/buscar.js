const express = require('express'); const router = express.Router(); const supabase = require('../../database/supabase'); router.post('/', async (req, res) => { const { query } = req.body; if (!query) return res.status(400).json({ error: 'Falta el término de búsqueda' }); try { const { data, error } = await supabase.from('productos').select('*').ilike('nombre', \%%\); if (error) throw error; res.json({ productos: data, sustitutos: [] }); } catch (error) { res.status(500).json({ error: error.message }); } }); module.exports = router;
